; This path is deprecated. Use `invisible_ai_assistant.iss` in this folder.
; It defines a stable AppId, accepts /DMyAppVersion=x.y.z, writes to ..\dist\, and supports in-place upgrades.
;
; Example:
;   ISCC.exe invisible_ai_assistant.iss /DMyAppVersion=1.2.3
#error Use invisible_ai_assistant.iss (see comments in that file).
